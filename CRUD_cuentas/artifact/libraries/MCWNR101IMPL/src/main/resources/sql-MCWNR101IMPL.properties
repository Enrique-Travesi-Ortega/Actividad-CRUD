check.status.by.accountNumber = gmcwn; SELECT STATUS FROM T_GMCWN_ACCOUNTS WHERE ACCOUNT_NUMBER = :accountNumber;
check.status.by.accountCard = gmcwn; SELECT STATUS FROM T_GMCWN_ACCOUNTS WHERE CARD_NUMBER = :accountCard;
insert.account = gmcwn; INSERT INTO T_GMCWN_ACCOUNTS (ACCOUNT_NUMBER, CARD_NUMBER, BALANCE, NIP, FIRST_NAME, LAST_NAME, AGE, CURP, RFC,CLIENT_TYPE, STATUS) VALUES (:ACCOUNT_NUMBER, :CARD_NUMBER, :BALANCE, :NIP, :FIRST_NAME, :LAST_NAME, :AGE, :CURP, :RFC, :CLIENT_TYPE, :STATUS);
select.accounts.by.status = gmcwn; SELECT ID , ACCOUNT_NUMBER , CARD_NUMBER , BALANCE , NIP , FIRST_NAME , LAST_NAME, AGE , CURP , RFC , CLIENT_TYPE , STATUS FROM T_GMCWN_ACCOUNTS WHERE STATUS BETWEEN :minStatus AND :maxStatus;
select.account.exists.by.accountNumber = gmcwn; SELECT ACCOUNT_NUMBER FROM T_GMCWN_ACCOUNTS WHERE ACCOUNT_NUMBER = :accountNumber;
select.account.status.by.accountNumber = gmcwn; SELECT STATUS FROM T_GMCWN_ACCOUNTS WHERE ACCOUNT_NUMBER = :accountNumber;
update.account.status.by.accountNumber = gmcwn; UPDATE T_GMCWN_ACCOUNTS SET STATUS = :status WHERE ACCOUNT_NUMBER = :accountNumber;
select.account.by.accountNumber = gmcwn; SELECT ID , ACCOUNT_NUMBER , CARD_NUMBER , BALANCE , NIP , FIRST_NAME , LAST_NAME, AGE , CURP , RFC , CLIENT_TYPE , STATUS FROM GMCWN.T_GMCWN_ACCOUNTS WHERE ACCOUNT_NUMBER = :accountNumber;
update.account.by.accountNumber = gmcwn; UPDATE GMCWN.T_GMCWN_ACCOUNTS SET FIRST_NAME=CASE WHEN :FIRST_NAME IS NOT NULL THEN :FIRST_NAME ELSE FIRST_NAME END,LAST_NAME=CASE WHEN :LAST_NAME IS NOT NULL THEN :LAST_NAME ELSE LAST_NAME END,RFC=CASE WHEN :RFC IS NOT NULL THEN :RFC ELSE RFC END,CURP=CASE WHEN :CURP IS NOT NULL THEN :CURP ELSE CURP END,AGE=CASE WHEN :AGE IS NOT NULL THEN TO_NUMBER(:AGE) ELSE AGE END,NIP=CASE WHEN :NIP IS NOT NULL THEN TO_NUMBER(:NIP) ELSE NIP END,CARD_NUMBER=CASE WHEN :CARD_NUMBER IS NOT NULL THEN TO_NUMBER(:CARD_NUMBER) ELSE CARD_NUMBER END,BALANCE=CASE WHEN :BALANCE IS NOT NULL THEN TO_NUMBER(:BALANCE) ELSE BALANCE END,CLIENT_TYPE=CASE WHEN :CLIENT_TYPE IS NOT NULL THEN TO_NUMBER(:CLIENT_TYPE) ELSE CLIENT_TYPE END WHERE ACCOUNT_NUMBER=:ACCOUNT_NUMBER AND ((:FIRST_NAME IS NOT NULL AND (FIRST_NAME<>TO_CHAR(:FIRST_NAME) OR (FIRST_NAME IS NULL AND :FIRST_NAME IS NOT NULL))) OR (:LAST_NAME IS NOT NULL AND (LAST_NAME<>TO_CHAR(:LAST_NAME) OR (LAST_NAME IS NULL AND :LAST_NAME IS NOT NULL))) OR (:RFC IS NOT NULL AND (RFC<>TO_CHAR(:RFC) OR (RFC IS NULL AND :RFC IS NOT NULL))) OR (:CURP IS NOT NULL AND (CURP<>TO_CHAR(:CURP) OR (CURP IS NULL AND :CURP IS NOT NULL))) OR (:AGE IS NOT NULL AND (AGE<>TO_NUMBER(:AGE) OR (AGE IS NULL AND :AGE IS NOT NULL))) OR (:NIP IS NOT NULL AND (NIP<>TO_NUMBER(:NIP) OR (NIP IS NULL AND :NIP IS NOT NULL))) OR (:CARD_NUMBER IS NOT NULL AND (CARD_NUMBER<>TO_NUMBER(:CARD_NUMBER) OR (CARD_NUMBER IS NULL AND :CARD_NUMBER IS NOT NULL))) OR (:BALANCE IS NOT NULL AND (BALANCE<>TO_NUMBER(:BALANCE) OR (BALANCE IS NULL AND :BALANCE IS NOT NULL))) OR (:CLIENT_TYPE IS NOT NULL AND (CLIENT_TYPE<>TO_NUMBER(:CLIENT_TYPE) OR (CLIENT_TYPE IS NULL AND :CLIENT_TYPE IS NOT NULL))))